server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: never

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/qingqingketang?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Abcd1234!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    time-zone: Asia/Shanghai
  sql:
    init:
      mode: always
      continue-on-error: true

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

schedule:
  ai:
    enabled: true
    base-url: https://api.scnet.cn/api/llm/v1
    api-key: 
    model: Qwen3-30B-A3B-Instruct-2507
    timeout-ms: ${SCHEDULE_AI_TIMEOUT_MS:20000}